\documentclass{article}
\usepackage[utf8]{inputenc}
\usepackage{hyperref}

\usepackage{xcolor}
\usepackage{pagecolor}
\usepackage{lipsum}  
\usepackage{mdframed}

\pagecolor{black}
\color{green}

\usepackage{amssymb} %maths
\usepackage{amsmath} %maths

\title{COMP4670/COMP8600 \\ Statistical Machine Learning - Sem 1 2022}
\author{u6451429}

\begin{document}
	
	\maketitle
	
	\tableofcontents
	
	\pagebreak
	\section{INTRODUCTION}
	
	polynomial curve fitting
	$$
	y(x, \mathbf{w})=w_{0}+w_{1} x+w_{2} x^{2}+\ldots+w_{M} x^{M}=\sum_{j=0}^{M} w_{j} x^{j}
	$$
	error function: mean squared error (MSE)
	$$
	E(\mathbf{w})=\frac{1}{2} \sum_{n=1}^{N}\left\{y\left(x_{n}, \mathbf{w}\right)-t_{n}\right\}^{2}
	$$
	root-mean-square (RMS) error
	$$
	E_{\mathrm{RMS}}=\sqrt{2 E\left(\mathbf{w}^{\star}\right) / N}
	$$
	Tikhonov regularization; shrinkage (statistics); ridge regression; weight decay (neural network)
	$$
	\widetilde{E}(\mathbf{w})=\frac{1}{2} \sum_{n=1}^{N}\left\{y\left(x_{n}, \mathbf{w}\right)-t_{n}\right\}^{2}+\frac{\lambda}{2}\|\mathbf{w}\|^{2}
	$$
	the squared Euclidean norm of the coefficients
	$$
	\|\mathbf{w}\|^{2} \equiv \mathbf{w}^{\mathrm{T}} \mathbf{w}=w_{0}^{2}+w_{1}^{2}+\ldots+w_{M}^{2}
	$$
	sum rule of probability
	$$
	p(X)=\sum_{Y} p(X, Y)\text{ or } p(x)=\int p(x, y) \mathrm{d} y
	$$
	product rule of probability
	$$
	p(X, Y)=p(Y \mid X) p(X) \text{ or } p(x, y)=p(y \mid x) p(x)
	$$
	conditional probability
	$$
	p(Y \mid X)=\frac{p(X \mid Y) p(Y)}{p(X)}
	$$
	Kolmogorov axioms; probability axioms 
	$$
	\begin{aligned}
	p(x) & \geqslant 0 \\
	\int_{-\infty}^{\infty} p(x) \mathrm{d} x &=1
	\end{aligned}
	$$
	Jacobian transform; Jacobian factor
	$$
	\begin{aligned}
	x&=g(y)\\
	p_{y}(y) &=p_{x}(x)\left|\frac{\mathrm{d} x}{\mathrm{~d} y}\right| \\
	&=p_{x}(g(y))\left|g^{\prime}(y)\right| 
	\end{aligned}
	$$
	
	$$
	\begin{array}{l}
	\mathbf{x} \sim \mathcal{N}(\boldsymbol{\mu}, \boldsymbol{\Sigma}), \mathbf{y} \sim \mathbf{A x}+\mathbf{b} \\
	\Rightarrow \mathbf{y} \sim \mathcal{N}\left(\mathbf{A} \boldsymbol{\mu}+\mathbf{b}, \mathbf{A} \boldsymbol{\Sigma} \mathbf{A}^{\top}\right)
	\end{array}
	$$	
	expectation of $f(x)$
	$$
	\mathbb{E}[f]=\sum_{x} p(x) f(x)=\int p(x) f(x) \mathrm{d} x
	$$
	variance of $f(x)$
	$$
	\operatorname{var}[f]=\mathbb{E}\left[(f(x)-\mathbb{E}[f(x)])^{2}\right]
	$$
	covariance of $x$ and $y$
	$$
	\begin{aligned}
	\operatorname{cov}[x, y] &=\mathbb{E}_{x, y}[\{x-\mathbb{E}[x]\}\{y-\mathbb{E}[y]\}] \\
	&=\mathbb{E}_{x, y}[x y]-\mathbb{E}[x] \mathbb{E}[y]
	\end{aligned}
	$$
	$$
	\begin{aligned}
	\operatorname{cov}[\mathbf{x}, \mathbf{y}] &=\mathbb{E}_{\mathbf{x}, \mathbf{y}}\left[\{\mathbf{x}-\mathbb{E}[\mathbf{x}]\}\left\{\mathbf{y}^{\mathrm{T}}-\mathbb{E}\left[\mathbf{y}^{\mathrm{T}}\right]\right\}\right] \\
	&=\mathbb{E}_{\mathbf{x}, \mathbf{y}}\left[\mathbf{x} \mathbf{y}^{\mathrm{T}}\right]-\mathbb{E}[\mathbf{x}] \mathbb{E}\left[\mathbf{y}^{\mathrm{T}}\right]
	\end{aligned}
	$$
	Bayes’ theorem
	$$
	p(\mathbf{w} \mid \mathcal{D})=\frac{p(\mathcal{D} \mid \mathbf{w}) p(\mathbf{w})}{p(\mathcal{D})}
	$$
	$$
	\text { posterior } \propto \text { likelihood } \times \text { prior }
	$$
	Gaussian distribution
	$$
	\mathcal{N}\left(x \mid \mu, \sigma^{2}\right)=\frac{1}{\left(2 \pi \sigma^{2}\right)^{1 / 2}} \exp \left\{-\frac{1}{2 \sigma^{2}}(x-\mu)^{2}\right\}
	$$
	$$
	\mathbb{E}[x]=\int_{-\infty}^{\infty} \mathcal{N}\left(x \mid \mu, \sigma^{2}\right) x \mathrm{~d} x=\mu
	$$
	$$
	\mathbb{E}\left[x^{2}\right]=\int_{-\infty}^{\infty} \mathcal{N}\left(x \mid \mu, \sigma^{2}\right) x^{2} \mathrm{~d} x=\mu^{2}+\sigma^{2}
	$$
	$$
	\operatorname{var}[x]=\mathbb{E}\left[x^{2}\right]-\mathbb{E}[x]^{2}=\sigma^{2}
	$$
	Gaussian integral
	$$
	{\displaystyle \int _{-\infty }^{\infty }e^{-a(x+b)^{2}}\,dx={\sqrt {\frac {\pi }{a}}}}
	$$
	multivariate Gaussian distribution
	$$
	\mathcal{N}(\mathbf{x} \mid \boldsymbol{\mu}, \boldsymbol{\Sigma})=\frac{1}{(2 \pi)^{D / 2}} \frac{1}{|\boldsymbol{\Sigma}|^{1 / 2}} \exp \left\{-\frac{1}{2}(\mathbf{x}-\boldsymbol{\mu})^{\mathrm{T}} \boldsymbol{\Sigma}^{-1}(\mathbf{x}-\boldsymbol{\mu})\right\}
	$$
	independent and identically distributed Gaussian random variables
	$$
	p\left(\mathbf{x} \mid \mu, \sigma^{2}\right)=\prod_{n=1}^{N} \mathcal{N}\left(x_{n} \mid \mu, \sigma^{2}\right)
	$$
	the log likelihood of Gaussian random variables
	$$
	\ln p\left(\mathbf{x} \mid \mu, \sigma^{2}\right)=-\frac{1}{2 \sigma^{2}} \sum_{n=1}^{N}\left(x_{n}-\mu\right)^{2}-\frac{N}{2} \ln \sigma^{2}-\frac{N}{2} \ln (2 \pi)
	$$
	the maximum likelihood solution
	$$
	\mu_{\mathrm{ML}}=\frac{1}{N} \sum_{n=1}^{N} x_{n}
	$$
	$$
	\sigma_{\mathrm{ML}}^{2}=\frac{1}{N} \sum_{n=1}^{N}\left(x_{n}-\mu_{\mathrm{ML}}\right)^{2}
	$$
	population mean and population variance 
	$$
	\begin{array}{l}
	\mathbb{E}\left[\mu_{\mathrm{ML}}\right]=\mu \\
	\mathbb{E}\left[\sigma_{\mathrm{ML}}^{2}\right]=\left(\frac{N-1}{N}\right) \sigma^{2}
	\end{array}
	$$
	unbiased estimate for the variance parameter
	$$
	\widetilde{\sigma}^{2}=\frac{N}{N-1} \sigma_{\mathrm{ML}}^{2}=\frac{1}{N-1} \sum_{n=1}^{N}\left(x_{n}-\mu_{\mathrm{ML}}\right)^{2}
	$$
	polynomial curve fitting
	$$
	p(t \mid x, \mathbf{w}, \beta)=\mathcal{N}\left(t \mid y(x, \mathbf{w}), \beta^{-1}\right)
	$$
	the log likelihood function
	$$
	\ln p(\mathbf{t} \mid \mathbf{x}, \mathbf{w}, \beta)=-\frac{\beta}{2} \sum_{n=1}^{N}\left\{y\left(x_{n}, \mathbf{w}\right)-t_{n}\right\}^{2}+\frac{N}{2} \ln \beta-\frac{N}{2} \ln (2 \pi)
	$$
	the maximum likelihood solution of the precision parameter $\beta$
	$$
	\frac{1}{\beta_{\mathrm{ML}}}=\frac{1}{N} \sum_{n=1}^{N}\left\{y\left(x_{n}, \mathbf{w}_{\mathrm{ML}}\right)-t_{n}\right\}^{2}
	$$
	maximizing likelihood is equivalent, so far as determining $\mathbf{w}$ is concerned, to minimizing the sum-of-squares error function
	$$
	\mathbf{w}_{\mathrm{ML}}\equiv\arg\min_{\mathbf{w}}E(\mathbf{w})=
	\frac{1}{2} \sum_{n=1}^{N}\left\{y\left(x_{n}, \mathbf{w}\right)-t_{n}\right\}^{2}
	$$
	predictive distribution via the maximum likelihood parameters
	$$
	p\left(t \mid x, \mathbf{w}_{\mathrm{ML}}, \beta_{\mathrm{ML}}\right)=\mathcal{N}\left(t \mid y\left(x, \mathbf{w}_{\mathrm{ML}}\right), \beta_{\mathrm{ML}}^{-1}\right)
	$$
	prior distribution over the polynomial coefficients $\mathbf{w}$
	$$
	p(\mathbf{w} \mid \alpha)=\mathcal{N}\left(\mathbf{w} \mid \mathbf{0}, \alpha^{-1} \mathbf{I}\right)=\left(\frac{\alpha}{2 \pi}\right)^{(M+1) / 2} \exp \left\{-\frac{\alpha}{2} \mathbf{w}^{\mathrm{T}} \mathbf{w}\right\}
	$$
	the posterior distribution for $\mathbf{w}$
	$$
	p(\mathbf{w} \mid \mathbf{x}, \mathbf{t}, \alpha, \beta) \propto p(\mathbf{t} \mid \mathbf{x}, \mathbf{w}, \beta) p(\mathbf{w} \mid \alpha)
	$$
	maximum a posteriori probability (MAP) estimate for $\mathbf{w}$
	\\
	maximizing the posterior distribution is equivalent to minimizing the regularized sum-of-squares error function, where $\lambda=\alpha/\beta$
	$$
	\mathbf{w}_{\mathrm{MAP}}\equiv\arg\min_{\mathbf{w}}\frac{\beta}{2} \sum_{n=1}^{N}\left\{y\left(x_{n}, \mathbf{w}\right)-t_{n}\right\}^{2}+\frac{\alpha}{2} \mathbf{w}^{\mathrm{T}} \mathbf{w}
	$$
	predictive distribution (Gaussian)
	$$
	p(t \mid x, \mathbf{x}, \mathbf{t})=\int p(t \mid x, \mathbf{w}) p(\mathbf{w} \mid \mathbf{x}, \mathbf{t}) \mathrm{d} \mathbf{w}
	$$
	$$
	p(t \mid x, \mathbf{x}, \mathbf{t})=\mathcal{N}\left(t \mid m(x), s^{2}(x)\right)
	$$
	$$
	\begin{aligned}
	m(x) &=\beta \boldsymbol{\phi}(x)^{\mathrm{T}} \mathbf{S} \sum_{n=1}^{N} \boldsymbol{\phi}\left(x_{n}\right) t_{n} \\
	s^{2}(x) &=\beta^{-1}+\boldsymbol{\phi}(x)^{\mathrm{T}} \mathbf{S} \boldsymbol{\phi}(x) \\
	\mathbf{S}^{-1}&=\alpha \mathbf{I}+\beta \sum_{n=1}^{N} \boldsymbol{\phi}\left(x_{n}\right) \boldsymbol{\phi}(x)^{\mathrm{T}}\\
	\boldsymbol{\phi}(x)&:=\phi_i(x)=x^i \text{ for }i=0,\dots,M
	\end{aligned}
	$$
	Akaike information criterion (AIC)
	$$
	\ln p\left(\mathcal{D} \mid \mathbf{w}_{\mathrm{ML}}\right)-M
	$$
	the expected loss 
	$$
	\mathbb{E}[L]=\iint L(t, y(\mathbf{x})) p(\mathbf{x}, t) \mathrm{d} \mathbf{x} \mathrm{d} t
	$$
	$$
	\mathbb{E}[L]=\iint\{y(\mathbf{x})-t\}^{2} p(\mathbf{x}, t) \mathrm{d} \mathbf{x} \mathrm{d} t
	$$
	Minkowski loss
	$$
	\mathbb{E}\left[L_{q}\right]=\iint|y(\mathbf{x})-t|^{q} p(\mathbf{x}, t) \mathrm{d} \mathbf{x} \mathrm{d} t
	$$
	
	\pagebreak
	
	${}$\\
	entropy
	$$
	\mathrm{H}[p]=-\sum_{i} p\left(x_{i}\right) \ln p\left(x_{i}\right)
	$$
	differential entropy
	$$
	\mathrm{H}[x]=-\int p(x) \ln p(x) \mathrm{d} x\quad \mathrm{H}[\mathbf{x}]=-\int p(\mathbf{x}) \ln p(\mathbf{x}) \mathrm{d} \mathbf{x}
	$$
	Stirling’s approximation
	$$
	\ln N ! \simeq N \ln N-N
	$$
	conditional entropy
	$$
	\mathrm{H}[\mathbf{y} \mid \mathbf{x}]=-\iint p(\mathbf{y}, \mathbf{x}) \ln p(\mathbf{y} \mid \mathbf{x}) \mathrm{d} \mathbf{y} \mathrm{d} \mathbf{x}
	$$
	$$
	\mathrm{H}[\mathbf{x}, \mathbf{y}]=\mathrm{H}[\mathbf{y} \mid \mathbf{x}]+\mathrm{H}[\mathbf{x}]
	$$
	Kullback-Leibler divergence (relative entropy)
	$$
	\begin{aligned}
	\mathrm{KL}(p \| q) &=-\int p(\mathbf{x}) \ln q(\mathbf{x}) \mathrm{d} \mathbf{x}-\left(-\int p(\mathbf{x}) \ln p(\mathbf{x}) \mathrm{d} \mathbf{x}\right) \\
	&=-\int p(\mathbf{x}) \ln \left\{\frac{q(\mathbf{x})}{p(\mathbf{x})}\right\} \mathrm{d} \mathbf{x} 
	\end{aligned}
	$$
	$$
	\mathrm{KL}(p \| q) \not \equiv \mathrm{KL}(q \| p)
	$$
	Jensen’s inequality
	$$
	f\left(\sum_{i=1}^{M} \lambda_{i} x_{i}\right) \leqslant \sum_{i=1}^{M} \lambda_{i} f\left(x_{i}\right)
	$$
	$$
	f(\mathbb{E}[x]) \leqslant \mathbb{E}[f(x)]
	$$
	$$
	f\left(\int \mathbf{x} p(\mathbf{x}) \mathrm{d} \mathbf{x}\right) \leqslant \int f(\mathbf{x}) p(\mathbf{x}) \mathrm{d} \mathbf{x}
	$$
	$$
	\mathrm{KL}(p \| q)=-\int p(\mathbf{x}) \ln \left\{\frac{q(\mathbf{x})}{p(\mathbf{x})}\right\} \mathrm{d} \mathbf{x} \geqslant-\ln \int q(\mathbf{x}) \mathrm{d} \mathbf{x}=0
	$$
	mutual information
	$$
	\begin{aligned}
	\mathrm{I}[\mathbf{x}, \mathbf{y}] & \equiv \operatorname{KL}(p(\mathbf{x}, \mathbf{y}) \| p(\mathbf{x}) p(\mathbf{y})) \\
	&=-\iint p(\mathbf{x}, \mathbf{y}) \ln \left(\frac{p(\mathbf{x}) p(\mathbf{y})}{p(\mathbf{x}, \mathbf{y})}\right) \mathrm{d} \mathbf{x} \mathrm{d} \mathbf{y} \\
	\mathrm{I}[\mathbf{x}, \mathbf{y}]&=\mathrm{H}[\mathbf{x}]-\mathrm{H}[\mathbf{x} \mid \mathbf{y}]=\mathrm{H}[\mathbf{y}]-\mathrm{H}[\mathbf{y} \mid \mathbf{x}]
	\end{aligned}
	$$
	
	\pagebreak
	\section{PROBABILITY DISTRIBUTIONS}
	Bernoulli distribution
	$$
	\operatorname{Bern}(x \mid \mu)=\mu^{x}(1-\mu)^{1-x}
	$$
	binomial distribution
	$$
	\operatorname{Bin}(m \mid N, \mu)=\binom{N}{m} \mu^{m}(1-\mu)^{N-m}
	$$
	$$
	\begin{aligned}
	\mathbb{E}[m] \equiv \sum_{m=0}^{N} m \operatorname{Bin}(m \mid N, \mu) &=N \mu \\
	\operatorname{var}[m] \equiv \sum_{m=0}^{N}(m-\mathbb{E}[m])^{2} \operatorname{Bin}(m \mid N, \mu) &=N \mu(1-\mu)
	\end{aligned}
	$$
	beta distribution
	$$
	\begin{aligned}
	\operatorname{Beta}(\mu \mid a, b) &=\frac{\Gamma(a+b)}{\Gamma(a) \Gamma(b)} \mu^{a-1}(1-\mu)^{b-1}\\
	\mathbb{E}[\mu] &=\frac{a}{a+b} \\
	\operatorname{var}[\mu] &=\frac{a b}{(a+b)^{2}(a+b+1)}
	\end{aligned}
	$$
	Dirichlet distribution
	$$
	{\displaystyle f\left(x_{1},\ldots ,x_{K};\alpha _{1},\ldots ,\alpha _{K}\right)={\frac {1}{\mathrm {B} ({\boldsymbol {\alpha }})}}\prod _{i=1}^{K}x_{i}^{\alpha _{i}-1}}
	$$
	$$
	{\displaystyle \mathrm {B} ({\boldsymbol {\alpha }})={\frac {\prod \limits _{i=1}^{K}\Gamma (\alpha _{i})}{\Gamma \left(\sum \limits _{i=1}^{K}\alpha _{i}\right)}},\qquad {\boldsymbol {\alpha }}=(\alpha _{1},\ldots ,\alpha _{K})}
	$$
	$$
	{\displaystyle \sum _{i=1}^{K}x_{i}=1{\mbox{ and }}x_{i}\geq 0{\mbox{ for all }}i\in \{1,\dots ,K\}}
	$$
	Mahalanobis distance
	$$
	\Delta^{2}=(\mathbf{x}-\boldsymbol{\mu})^{\mathrm{T}} \boldsymbol{\Sigma}^{-1}(\mathbf{x}-\boldsymbol{\mu})
	$$
	Schur complement
	$$
	\left(\begin{array}{ll}
	\mathbf{A} & \mathbf{B} \\
	\mathbf{C} & \mathbf{D}
	\end{array}\right)^{-1}=\left(\begin{array}{cc}
	\mathbf{M} & -\mathbf{M B D}^{-1} \\
	-\mathbf{D}^{-1} \mathbf{C M} & \mathbf{D}^{-1}+\mathbf{D}^{-1} \mathbf{C M B D}^{-1} 
	\end{array}\right),\quad
	\mathbf{M}=\left(\mathbf{A}-\mathbf{B D}^{-1} \mathbf{C}\right)^{-1}
	$$
	Partitioned Gaussians
	$$
	\begin{aligned}
	\mathbf{x}=\left(\begin{array}{c}
	\mathbf{x}_{a} \\
	\mathbf{x}_{b}
	\end{array}\right)&, \quad \boldsymbol{\mu}=\left(\begin{array}{c}
	\boldsymbol{\mu}_{a} \\
	\boldsymbol{\mu}_{b}
	\end{array}\right)\\
	\boldsymbol{\Sigma}=\left(\begin{array}{ll}
	\boldsymbol{\Sigma}_{a a} & \boldsymbol{\Sigma}_{a b} \\
	\boldsymbol{\Sigma}_{b a} & \boldsymbol{\Sigma}_{b b}
	\end{array}\right)&, \quad \boldsymbol{\Lambda}=\left(\begin{array}{ll}
	\boldsymbol{\Lambda}_{a a} & \boldsymbol{\Lambda}_{a b} \\
	\boldsymbol{\Lambda}_{b a} & \boldsymbol{\Lambda}_{b b}
	\end{array}\right)
	\end{aligned}
	$$
	$$
	\begin{aligned}
	p\left(\mathbf{x}_{a} \mid \mathbf{x}_{b}\right) &=\mathcal{N}\left(\mathbf{x} \mid \boldsymbol{\mu}_{a \mid b}, \boldsymbol{\Lambda}_{a a}^{-1}\right) \\
	\boldsymbol{\mu}_{a \mid b} &=\boldsymbol{\mu}_{a}-\boldsymbol{\Lambda}_{a a}^{-1} \boldsymbol{\Lambda}_{a b}\left(\mathbf{x}_{b}-\boldsymbol{\mu}_{b}\right)\\
	p\left(\mathbf{x}_{a}\right)&=\mathcal{N}\left(\mathbf{x}_{a} \mid \boldsymbol{\mu}_{a}, \boldsymbol{\Sigma}_{a a}\right)
	\end{aligned}
	$$
	population mean and population variance 
	 $$
	 \begin{aligned}
	 \boldsymbol{\mu}_{\mathrm{ML}}&=\frac{1}{N} \sum_{n=1}^{N} \mathbf{x}_{n}\\
	 \boldsymbol{\Sigma}_{\mathrm{ML}}&=\frac{1}{N} \sum_{n=1}^{N}\left(\mathbf{x}_{n}-\boldsymbol{\mu}_{\mathrm{ML}}\right)\left(\mathbf{x}_{n}-\boldsymbol{\mu}_{\mathrm{ML}}\right)^{\mathrm{T}}
	 \end{aligned}
	 $$
	 
	$$
	\begin{aligned}
	\mathbb{E}\left[\boldsymbol{\mu}_{\mathrm{ML}}\right] &=\boldsymbol{\mu} \\
	\mathbb{E}\left[\boldsymbol{\Sigma}_{\mathrm{ML}}\right] &=\frac{N-1}{N} \boldsymbol{\Sigma}
	\end{aligned}
	$$
	unbiased estimator of the covariance matrix
	$$
	\widetilde{\boldsymbol{\Sigma}}=\frac{1}{N-1} \sum_{n=1}^{N}\left(\mathbf{x}_{n}-\boldsymbol{\mu}_{\mathrm{ML}}\right)\left(\mathbf{x}_{n}-\boldsymbol{\mu}_{\mathrm{ML}}\right)^{\mathrm{T}}
	$$
	Bayesian inference for the Gaussian of $\mu$
	$$
	p(\mathbf{X} \mid \mu)=\prod_{n=1}^{N} p\left(x_{n} \mid \mu\right)=\frac{1}{\left(2 \pi \sigma^{2}\right)^{N / 2}} \exp \left\{-\frac{1}{2 \sigma^{2}} \sum_{n=1}^{N}\left(x_{n}-\mu\right)^{2}\right\}
	$$
	$$
	\mathbf{X}=\left\{x_{1}, \ldots, x_{N}\right\}
	$$
	prior distribution of $\mu$
	$$
	p(\mu)=\mathcal{N}\left(\mu \mid \mu_{0}, \sigma_{0}^{2}\right)
	$$
	posterior distribution of $\mu$, where $\sigma^2$ is known
	$$
	p(\mu \mid \mathbf{X}) \propto p(\mathbf{X} \mid \mu) p(\mu)
	$$
	$$
	p(\mu \mid \mathbf{X})=\mathcal{N}\left(\mu \mid \mu_{N}, \sigma_{N}^{2}\right)
	$$
	$$
	\begin{aligned}
	\mu_{N} &=\frac{\sigma^{2}}{N \sigma_{0}^{2}+\sigma^{2}} \mu_{0}+\frac{N \sigma_{0}^{2}}{N \sigma_{0}^{2}+\sigma^{2}} \mu_{\mathrm{ML}} \\
	\frac{1}{\sigma_{N}^{2}} &=\frac{1}{\sigma_{0}^{2}}+\frac{N}{\sigma^{2}}\\
	\mu_{\mathrm{ML}}&=\frac{1}{N} \sum_{n=1}^{N} x_{n}
	\end{aligned}
	$$
	if $\sigma_{0}^{2} \rightarrow \infty$, then
	$$
	\mu_{N}=\mu_{\mathrm{ML}}
	$$
	$$
	\sigma_{N}^{2}=\sigma^{2} / N
	$$
	Bayesian inference for the Gaussian of $\lambda$
	$$
	p(\mathbf{X} \mid \lambda)=\prod_{n=1}^{N} \mathcal{N}\left(x_{n} \mid \mu, \lambda^{-1}\right) \propto \lambda^{N / 2} \exp \left\{-\frac{\lambda}{2} \sum_{n=1}^{N}\left(x_{n}-\mu\right)^{2}\right\}
	$$
	prior distribution of $\lambda$ (gamma distribution)
	$$
	\operatorname{Gam}(\lambda \mid a_0, b_0)=\frac{1}{\Gamma(a_0)} b_0^{a_0} \lambda^{a_0-1} \exp (-b_0 \lambda)
	$$
	$$
	p(\lambda \mid \mathbf{X}) \propto \lambda^{a_{0}-1} \lambda^{N / 2} \exp \left\{-b_{0} \lambda-\frac{\lambda}{2} \sum_{n=1}^{N}\left(x_{n}-\mu\right)^{2}\right\}
	$$
	posterior distribution of $\lambda$, where $\mu$ is known
	$$
	p(\lambda \mid \mathbf{X})=\operatorname{Gam}\left(\lambda \mid a_{N}, b_{N}\right)
	$$
	$$
	\begin{aligned}
	a_{N} &=a_{0}+\frac{N}{2} \\
	b_{N} &=b_{0}+\frac{1}{2} \sum_{n=1}^{N}\left(x_{n}-\mu\right)^{2}=b_{0}+\frac{N}{2} \sigma_{\mathrm{ML}}^{2} \\
	\sigma_{\mathrm{ML}}^{2}&=\frac{1}{N} \sum_{n=1}^{N}\left(x_{n}-\mu\right)^{2}
	\end{aligned}
	$$
	Bayesian inference for the Gaussian of $\mu$ and $\lambda$
	$$
	\begin{aligned}
	p(\mathbf{X} \mid \mu, \lambda)&=\prod_{n=1}^{N}\left(\frac{\lambda}{2 \pi}\right)^{1 / 2} \exp \left\{-\frac{\lambda}{2}\left(x_{n}-\mu\right)^{2}\right\} \\
	&\propto\left[\lambda^{1 / 2} \exp \left(-\frac{\lambda \mu^{2}}{2}\right)\right]^{N} \exp \left\{\lambda \mu \sum_{n=1}^{N} x_{n}-\frac{\lambda}{2} \sum_{n=1}^{N} x_{n}^{2}\right\}
	\end{aligned}
	$$
	prior distribution of $\mu$ and $\lambda$ 
	$$
	\begin{aligned}
	p(\mu, \lambda) &\propto\left[\lambda^{1 / 2} \exp \left(-\frac{\lambda \mu^{2}}{2}\right)\right]^{\beta} \exp \{c \lambda \mu-d \lambda\} \\
	&=\exp \left\{-\frac{\beta \lambda}{2}(\mu-c / \beta)^{2}\right\} \lambda^{\beta / 2} \exp \left\{-\left(d-\frac{c^{2}}{2 \beta}\right) \lambda\right\}
	\end{aligned}
	$$
	$$
	p(\mu, \lambda)=\mathcal{N}\left(\mu \mid \mu_{0},(\beta \lambda)^{-1}\right) \operatorname{Gam}(\lambda \mid a, b)
	$$
	$$
	\mu_{0}=c / \beta, \quad a=1+\beta / 2, \quad b=d-\frac{c^{2}}{2\beta}
	$$
	Bayesian inference for the multivariate Gaussian distribution for $\boldsymbol{\mu}$ \\
	known precision $\boldsymbol{\Lambda}$
	$$
	\mathcal{N}\left(\mathbf{x} \mid \boldsymbol{\mu}, \boldsymbol{\Lambda}^{-1}\right)
	$$
	Wishart prior distribution
	$$
	\mathcal{W}(\boldsymbol{\Lambda} \mid \mathbf{W}, \nu)=B|\boldsymbol{\Lambda}|^{(\nu-D-1) / 2} \exp \left(-\frac{1}{2} \operatorname{Tr}\left(\mathbf{W}^{-1} \boldsymbol{\Lambda}\right)\right)
	$$
	$$
	B(\mathbf{W}, \nu)=|\mathbf{W}|^{-\nu / 2}\left(2^{\nu D / 2} \pi^{D(D-1) / 4} \prod_{i=1}^{D} \Gamma\left(\frac{\nu+1-i}{2}\right)\right)^{-1}
	$$
	normal-Wishart (Gaussian-Wishart) prior distribution
	$$
	p\left(\boldsymbol{\mu}, \boldsymbol{\Lambda} \mid \boldsymbol{\mu}_{0}, \beta, \mathbf{W}, \nu\right)=\mathcal{N}\left(\boldsymbol{\mu} \mid \boldsymbol{\mu}_{0},(\beta \boldsymbol{\Lambda})^{-1}\right) \mathcal{W}(\boldsymbol{\Lambda} \mid \mathbf{W}, \nu)
	$$
	
	\pagebreak
	\section{LINEAR MODELS FOR REGRESSION}
	simple linear regression
	$$
	y(\mathbf{x}, \mathbf{w})=w_{0}+w_{1} x_{1}+\ldots+w_{D} x_{D}
	$$
	regression with basis functions
	$$
	y(\mathbf{x}, \mathbf{w})=w_{0}+\sum_{j=1}^{M-1} w_{j} \phi_{j}(\mathbf{x})
	$$
	$$
	y(\mathbf{x}, \mathbf{w})=\sum_{j=0}^{M-1} w_{j} \phi_{j}(\mathbf{x})=\mathbf{w}^{\mathrm{T}} \boldsymbol{\phi}(\mathbf{x})
	$$
	dummy basis function
	$$
	\phi_{0}(\mathbf{x})=1
	$$
	Gaussian basis function
	$$
	\phi_{j}(x)=\exp \left\{-\frac{\left(x-\mu_{j}\right)^{2}}{2 s^{2}}\right\}
	$$
	sigmoidal basis function 
	$$
	\phi_{j}(x)=\sigma\left(\frac{x-\mu_{j}}{s}\right)
	$$
	$$
	\sigma(a)=\frac{1}{1+\exp (-a)}
	$$
	$$
	\tanh (a)=2 \sigma(a)-1
	$$
	Gaussian noise model
	$$
	\begin{aligned}
	t&=y(\mathbf{x}, \mathbf{w})+\epsilon \\
	p(t \mid \mathbf{x}, \mathbf{w}, \beta)&=\mathcal{N}\left(t \mid y(\mathbf{x}, \mathbf{w}), \beta^{-1}\right)\\
	p(\mathbf{t} \mid \mathbf{X}, \mathbf{w}, \beta)&=\prod_{n=1}^{N} \mathcal{N}\left(t_{n} \mid \mathbf{w}^{\mathrm{T}} \boldsymbol{\phi}\left(\mathbf{x}_{n}\right), \beta^{-1}\right)\\
	\ln p(\mathbf{t} \mid \mathbf{w}, \beta) &=\sum_{n=1}^{N} \ln \mathcal{N}\left(t_{n} \mid \mathbf{w}^{\mathrm{T}} \boldsymbol{\phi}\left(\mathbf{x}_{n}\right), \beta^{-1}\right) \\
	&=\frac{N}{2} \ln \beta-\frac{N}{2} \ln (2 \pi)-\beta E_{D}(\mathbf{w})
	\end{aligned}
	$$
	where $E_{D}(\mathbf{w})$ is the sum-of-squares error function
	$$
	E_{D}(\mathbf{w})=\frac{1}{2} \sum_{n=1}^{N}\left\{t_{n}-\mathbf{w}^{\mathrm{T}} \boldsymbol{\phi}\left(\mathbf{x}_{n}\right)\right\}^{2}
	$$
	the maximum likelihood solution; differentiate with respect to $\mathbf{w}$
	$$
	\nabla \ln p(\mathbf{t} \mid \mathbf{w}, \beta)=\sum_{n=1}^{N}\left\{t_{n}-\mathbf{w}^{\mathrm{T}} \boldsymbol{\phi}\left(\mathbf{x}_{n}\right)\right\} \boldsymbol{\phi}\left(\mathbf{x}_{n}\right)^{\mathrm{T}}
	$$
	$$
	0=\sum_{n=1}^{N} t_{n} \boldsymbol{\phi}\left(\mathbf{x}_{n}\right)^{\mathrm{T}}-\mathbf{w}^{\mathrm{T}}\left(\sum_{n=1}^{N} \boldsymbol{\phi}\left(\mathbf{x}_{n}\right) \boldsymbol{\phi}\left(\mathbf{x}_{n}\right)^{\mathrm{T}}\right)
	$$
	$$
	\mathbf{w}_{\mathrm{ML}}=\left(\boldsymbol{\Phi}^{\mathrm{T}} \boldsymbol{\Phi}\right)^{-1} \boldsymbol{\Phi}^{\mathrm{T}} \mathbf{t}
	$$
	differentiate with respect to $\beta$
	$$
	\frac{1}{\beta_{\mathrm{ML}}}=\frac{1}{N} \sum_{n=1}^{N}\left\{t_{n}-\mathbf{w}_{\mathrm{ML}}^{\mathrm{T}} \boldsymbol{\phi}\left(\mathbf{x}_{n}\right)\right\}^{2}
	$$
	where $\boldsymbol{\Phi}$ is the design matrix; $
	\Phi_{n j}=\phi_{j}\left(\mathbf{x}_{n}\right)$ ($\text{matrix}_{\text{ row, col}}$)
	$$
	\boldsymbol{\Phi}=\left(\begin{array}{cccc}
	\phi_{0}\left(\mathbf{x}_{1}\right) & \phi_{1}\left(\mathbf{x}_{1}\right) & \cdots & \phi_{M-1}\left(\mathbf{x}_{1}\right) \\
	\phi_{0}\left(\mathbf{x}_{2}\right) & \phi_{1}\left(\mathbf{x}_{2}\right) & \cdots & \phi_{M-1}\left(\mathbf{x}_{2}\right) \\
	\vdots & \vdots & \ddots & \vdots \\
	\phi_{0}\left(\mathbf{x}_{N}\right) & \phi_{1}\left(\mathbf{x}_{N}\right) & \cdots & \phi_{M-1}\left(\mathbf{x}_{N}\right)
	\end{array}\right)
	$$
	Moore-Penrose pseudo-inverse of the matrix $\boldsymbol{\Phi}$
	$$
	\boldsymbol{\Phi}^{\dagger} \equiv\left(\boldsymbol{\Phi}^{\mathrm{T}} \boldsymbol{\Phi}\right)^{-1} \boldsymbol{\Phi}^{\mathrm{T}}
	$$
	if $\boldsymbol{\Phi}$ is square and invertible, then
	$$
	(\mathbf{A B})^{-1}=\mathbf{B}^{-1} \mathbf{A}^{-1}
	$$	
	$$(\boldsymbol{\Phi}^{\mathrm{T}} \boldsymbol{\Phi})^{-1}=\boldsymbol{\Phi}^{-1} (\boldsymbol{\Phi}^{\mathrm{T}})^{-1}
	$$
	$$(\boldsymbol{\Phi}^{\mathrm{T}} \boldsymbol{\Phi})^{-1}
	\boldsymbol{\Phi}^{\mathrm{T}}=\boldsymbol{\Phi}^{-1}((\boldsymbol{\Phi}^{\mathrm{T}})^{-1}\boldsymbol{\Phi}^{\mathrm{T}})=\boldsymbol{\Phi}^{-1}\mathbf{I}=\boldsymbol{\Phi}^{-1}
	$$
	$$
	\boldsymbol{\Phi}^{\dagger} \equiv \boldsymbol{\Phi}^{-1}
	$$
	
	\pagebreak
	\section{LINEAR MODELS FOR CLASSIFICATION}
	posterior distribution of discrete classes
	$$
	p\left(\mathcal{C}_{k} \mid \mathbf{x}\right)=\frac{p\left(\mathbf{x} \mid \mathcal{C}_{k}\right) p\left(\mathcal{C}_{k}\right)}{p(\mathbf{x})}
	$$
	transform the linear function of $\mathbf{w}$ using a nonlinear function $f(\cdot)$
	$$
	y(\mathbf{x})=f\left(\mathbf{w}^{\mathrm{T}} \mathbf{x}+w_{0}\right)
	$$
	two classes $\mathcal{C}_{1}$ and $\mathcal{C}_{2}$
	$$
	y(\mathbf{x})=\mathbf{w}^{\mathrm{T}} \mathbf{x}+w_{0}
	$$
	input vector $\mathbf{x}$ is assigned to class $\mathcal{C}_{1}$ if $y(\mathbf{x}) \geqslant 0$ and to class $\mathcal{C}_{2}$ otherwise.\\ \\
	In Euclidean space, two vectors are orthogonal iff their dot product is zero.\\ \\
	Consider two points $\mathbf{x}_{\mathrm{A}}$ and $\mathbf{x}_{\mathrm{B}}$ both of which lie on the decision surface, then $y\left(\mathbf{x}_{\mathrm{A}}\right)=y\left(\mathbf{x}_{\mathrm{B}}\right)=0$, $\mathbf{w}^{\mathrm{T}}\left(\mathbf{x}_{\mathrm{A}}-\mathbf{x}_{\mathrm{B}}\right)=0$ and hence the vector $\mathbf{w}$ is orthogonal to the decision surface. \\ \\ 
	the signed orthogonal distance of a general point $\mathbf{x}$ from the decision surface is given by $y(\mathbf{x}) /\|\mathbf{w}\|$.
	
	\pagebreak
	\section{NEURAL NETWORKS}
	
	 
	\pagebreak
	\section{KERNEL METHODS}
	kernel function
	$$
	k\left(\mathbf{x}, \mathbf{x}^{\prime}\right)=\boldsymbol{\phi}(\mathbf{x})^{\mathrm{T}} \boldsymbol{\phi}\left(\mathbf{x}^{\prime}\right)
	$$
	the kernel is a symmetric function of its arguments
	$$
	k\left(\mathbf{x}, \mathbf{x}^{\prime}\right)=k\left(\mathbf{x}^{\prime}, \mathbf{x}\right)
	$$
	reformulation of linear regression model with a regularized sum-of-squares error\\
	dual representations
	$$
	J(\mathbf{w})=\frac{1}{2} \sum_{n=1}^{N}\left\{\mathbf{w}^{\mathrm{T}} \boldsymbol{\phi}\left(\mathbf{x}_{n}\right)-t_{n}\right\}^{2}+\frac{\lambda}{2} \mathbf{w}^{\mathrm{T}} \mathbf{w}
	$$
	solve for $\mathbf{w}$
	$$
	\mathbf{w}=-\frac{1}{\lambda} \sum_{n=1}^{N}\left\{\mathbf{w}^{\mathrm{T}} \boldsymbol{\phi}\left(\mathbf{x}_{n}\right)-t_{n}\right\} \boldsymbol{\phi}\left(\mathbf{x}_{n}\right)=\sum_{n=1}^{N} a_{n} \boldsymbol{\phi}\left(\mathbf{x}_{n}\right)=\mathbf{\Phi}^{\mathrm{T}} \mathbf{a}
	$$
	$$
	a_{n}=-\frac{1}{\lambda}\left\{\mathbf{w}^{\mathrm{T}} \boldsymbol{\phi}\left(\mathbf{x}_{n}\right)-t_{n}\right\}
	$$
	where $\Phi$ is the design matrix, whose $n^{\text {th }}$ row is given by $\boldsymbol{\phi}\left(\mathbf{x}_{n}\right)^{\mathrm{T}}$\\
	substitute ``$\mathbf{w}=\boldsymbol{\Phi}^{\mathrm{T}} \mathbf{a}$" into $J(\mathbf{w})$
	$$
	J(\mathbf{a})=\frac{1}{2} \mathbf{a}^{\mathrm{T}} \boldsymbol{\Phi} \boldsymbol{\Phi}^{\mathrm{T}} \boldsymbol{\Phi} \boldsymbol{\Phi}^{\mathrm{T}} \mathbf{a}-\mathbf{a}^{\mathrm{T}} \boldsymbol{\Phi} \boldsymbol{\Phi}^{\mathrm{T}} \mathbf{t}+\frac{1}{2} \mathbf{t}^{\mathrm{T}} \mathbf{t}+\frac{\lambda}{2} \mathbf{a}^{\mathrm{T}} \boldsymbol{\Phi} \boldsymbol{\Phi}^{\mathrm{T}} \mathbf{a}
	$$
	Gram matrix ($\boldsymbol{\Phi} \in \mathbb{R}^{N\times M}$, $
	\mathbf{K} \in \mathbb{R}^{N\times N}$)
	$$
	\mathbf{K}=\boldsymbol{\Phi} \boldsymbol{\Phi}^{\mathrm{T}}
	$$
	$$
	K_{n m}=\boldsymbol{\phi}\left(\mathbf{x}_{n}\right)^{\mathrm{T}} \boldsymbol{\phi}\left(\mathbf{x}_{m}\right)=k\left(\mathbf{x}_{n}, \mathbf{x}_{m}\right)
	$$
	substitute ``$\mathbf{K}=\boldsymbol{\Phi} \boldsymbol{\Phi}^{\mathrm{T}}$" into $J(\mathbf{a})$
	$$
	J(\mathbf{a})=\frac{1}{2} \mathbf{a}^{\mathrm{T}} \mathbf{K K} \mathbf{a}-\mathbf{a}^{\mathrm{T}} \mathbf{K} \mathbf{t}+\frac{1}{2} \mathbf{t}^{\mathrm{T}} \mathbf{t}+\frac{\lambda}{2} \mathbf{a}^{\mathrm{T}} \mathbf{K} \mathbf{a}
	$$
	solve for $\mathbf{a}$
	$$
	\mathbf{a}=\left(\mathbf{K}+\lambda \mathbf{I}_{N}\right)^{-1} \mathbf{t}
	$$
	substitute the solution into the linear regression model\\
	prediction for a new input $\mathbf{x}$
	$$
	y(\mathbf{x})=\mathbf{w}^{\mathrm{T}} \boldsymbol{\phi}(\mathbf{x})=\mathbf{a}^{\mathrm{T}} \boldsymbol{\Phi} \boldsymbol{\phi}(\mathbf{x})=\mathbf{k}(\mathbf{x})^{\mathrm{T}}\left(\mathbf{K}+\lambda \mathbf{I}_{N}\right)^{-1} \mathbf{t}
	$$
	$$
	\mathbf{k}(\mathbf{x})\in \mathbb{R}^N, \text{ where } k_{n}(\mathbf{x})=k\left(\mathbf{x}_{n}, \mathbf{x}\right)=\boldsymbol{\phi}(\mathbf{x}_n)^{\mathrm{T}} \boldsymbol{\phi}\left(\mathbf{x}\right)
	$$
	
	\pagebreak
	
	${}$\\
	kernel construction
	$$
	k(\mathbf{x}, \mathbf{z})=\left(\mathbf{x}^{\mathrm{T}} \mathbf{z}\right)^{2}
	$$
	where 
	$$
	\begin{aligned}
	k(\mathbf{x}, \mathbf{z}) &=\left(\mathbf{x}^{\mathrm{T}} \mathbf{z}\right)^{2}=\left(x_{1} z_{1}+x_{2} z_{2}\right)^{2} \\
	&=x_{1}^{2} z_{1}^{2}+2 x_{1} z_{1} x_{2} z_{2}+x_{2}^{2} z_{2}^{2} \\
	&=\left(x_{1}^{2}, \sqrt{2} x_{1} x_{2}, x_{2}^{2}\right)\left(z_{1}^{2}, \sqrt{2} z_{1} z_{2}, z_{2}^{2}\right)^{\mathrm{T}} \\
	&=\phi(\mathbf{x})^{\mathrm{T}} \boldsymbol{\phi}(\mathbf{z})
	\end{aligned}
	$$
	$$
	\phi(\mathbf{x})=\left(x_{1}^{2}, \sqrt{2} x_{1} x_{2}, x_{2}^{2}\right)^{\mathrm{T}}
	$$
	compute $\left(\mathbf{x}^{\mathrm{T}} \mathbf{z}\right)^{2}$ instead of explicitly applying the transformations\\
	which is less computationally expensive 
	$$
	k\left(\mathbf{x}, \mathbf{x}^{\prime}\right) \text { is a valid kernel }
	$$
	$$
	\leftrightarrow
	$$
	$$
	\text { the Gram matrix } \mathbf{K}\text { is a positive semidefinite}
	$$
	$$
	\text{for all possible choices of the set }\left\{\mathbf{x}_{n}\right\}
	$$
	
	\pagebreak
	\section{SPARSE KERNEL MACHINES}
	
	\pagebreak
	\section{GRAPHICAL MODELS}
	
	\pagebreak
	\section{MIXTURE MODELS AND EM}
	objective function (distortion measure)
	$$
	J=\sum_{n=1}^{N} \sum_{k=1}^{K} r_{n k}\left\|\mathbf{x}_{n}-\boldsymbol{\mu}_{k}\right\|^{2}
	$$
	where
	$$
	r_{n k}=\left\{\begin{array}{ll}
	1 & \text { if } k=\arg \min _{j}\left\|\mathbf{x}_{n}-\boldsymbol{\mu}_{j}\right\|^{2} \\
	0 & \text { otherwise }
	\end{array}\right.
	$$
	differentiate the objective function $J$ with respect to $\boldsymbol{\mu}_{k}$ for every $k$\\setting the derivative to zero
	$$
	2 \sum_{n=1}^{N} r_{n k}\left(\mathbf{x}_{n}-\boldsymbol{\mu}_{k}\right)=0
	$$
	solve for $\boldsymbol{\mu}_{k}$
	$$
	\boldsymbol{\mu}_{k}=\frac{\sum_{n} r_{n k} \mathbf{x}_{n}}{\sum_{n} r_{n k}}
	$$
	objective function; where $
	\mathcal{V}\left(\mathbf{x}, \mathbf{x}^{\prime}\right)
	$ is a general dissimilarity measure 
	$$
	\widetilde{J}=\sum_{n=1}^{N} \sum_{k=1}^{K} r_{n k} \mathcal{V}\left(\mathbf{x}_{n}, \boldsymbol{\mu}_{k}\right)
	$$
	Gaussian mixture model
	$$
	p(\mathbf{x})=\sum_{k=1}^{K} \pi_{k} \mathcal{N}\left(\mathbf{x} \mid \boldsymbol{\mu}_{k}, \boldsymbol{\Sigma}_{k}\right)
	$$
	
	\pagebreak
	\section{APPROXIMATE INFERENCE}
	
	\pagebreak
	\section{SAMPLING METHODS}
	the expectation of function $f(\mathbf{z})$ with respect to a probability distribution $p(\mathbf{z})$
	$$
	\mathbb{E}[f]=\int f(\mathbf{z}) p(\mathbf{z}) \mathrm{d} \mathbf{z}
	$$
	expectation approximation by a finite sum
	$$
	\widehat{f}=\frac{1}{L} \sum_{l=1}^{L} f\left(\mathbf{z}^{(l)}\right)
	$$
	
	\pagebreak
	\section{CONTINUOUS LATENT VARIABLES}
	
\end{document}